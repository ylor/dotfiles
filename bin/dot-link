#!/usr/bin/env fish
if test (count $argv) -ne 1
    echo "Usage: dot-link [/path/to/source]"
    return 1
end

set orig (realpath $argv[1])
set link (string replace "$DOTFILES_PATH/home" "$HOME" "$orig")

mkdir -p (dirname $link) # stage folder
ln -sf $orig $link # link file
